# SPH demo

This demo contains two source files:

- `simulation.py` creates `warp.examples.core.example_sph.Example` without its
  USD renderer.
- `render_opengl.py` is both the executable entry point and the OpenGL renderer.

Run from the repository root:

```bash
python demos/fluid/sph/render_opengl.py
```
