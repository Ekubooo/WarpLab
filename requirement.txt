# Snapshot of the repository's .venv (Python 3.10.20).
# Generated from the packages currently installed in the project environment.

# Core simulation and the repository's OpenGL frontends.
numpy==2.2.6
pyglet==2.1.15
warp-lang==1.15.0

# Optional Warp example/renderer support. These packages are retained as a
# commented snapshot because the repository disables USD output and does not
# import the plotting, image, compression, or process-monitoring features.
# blosc==1.11.4
# matplotlib==3.10.9
# pillow==12.3.0
# psutil==7.2.2
# usd-core==26.8

# Indirect dependencies of the optional matplotlib package above.
# contourpy==1.3.2
# cycler==0.12.1
# fonttools==4.63.0
# kiwisolver==1.5.0
# packaging==26.2
# pyparsing==3.3.2
# python-dateutil==2.9.0.post0
# six==1.17.0
